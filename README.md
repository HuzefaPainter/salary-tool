Things to note clearly for later:
Separate sessions and registrations controller because even though they are both associated with auth, keeping them separate separates the login/logout of existing user and registeration of new users.
